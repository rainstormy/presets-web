{
	"$schema": "https://json.schemastore.org/package.json",
	"name": "@rainstormy/preset-eslint-react",
	"version": "0.0.1-alpha.0",
	"type": "module",
	"main": "dist/eslint.config.js",
	"types": "dist/eslint.config.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build.dist": "vite build",
		"build.pack": "yarn pack --out %s-%v.tgz"
	},
	"dependencies": {
		"@rainstormy/preset-eslint-jsx": "workspace:*",
		"eslint-plugin-react-hooks": "4.6.0"
	},
	"devDependencies": {
		"@types/eslint": "8.44.7",
		"typescript": "5.2.2",
		"vite": "4.5.0",
		"vite-plugin-dts": "3.6.3"
	},
	"peerDependencies": {
		"eslint": "8.x"
	},
	"//": {
		"dependencies": {
			"@rainstormy/preset-eslint-jsx": "An inheritable ESLint configuration for JSX- and React-like components.",
			"eslint-plugin-react-hooks": "An ESLint plugin that covers the Rules of Hooks in React-like components."
		},
		"devDependencies": {
			"@types/eslint": "A set of type definitions for ESLint configuration files.",
			"typescript": "A statically typed programming language that extends the JavaScript syntax.",
			"vite": "A highly extensible build tool.",
			"vite-plugin-dts": "A Vite plugin that generates TypeScript declaration files."
		},
		"peerDependencies": {
			"eslint": "A static program analysis tool that uncovers code issues."
		}
	}
}
